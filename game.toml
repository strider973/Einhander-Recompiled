# Autofilled by tools/new_project_layout/probe_disc.py from your legal dump.
# Review before shipping. Runtime quirks / overlay seeds still need hand work.

[game]
name = "Einhander"
id = "SCUS-94243"
players = 1
exe = "disc/SCUS_942.43"
disc = "D:\\PS1RECOMP - CLAUDE\\Einhaender (USA).cue"
load_address = "0x80010000"
entry_pc = "0x800155FC"
text_size = "0x00022000"
stack_base = "0x801FFFF0"

# Digests are for data Track 01 (first BINARY FILE / TRACK 01).
[prepare_disc]
out_dir = "disc"
bin_name = "Einhaender (USA).bin"
cue_name = "Einhaender (USA).cue"
boot_exe = "SCUS_942.43"
known_sizes = [653493792]
known_md5 = [
  "a3aa50b173f19c6d2da6ae2e970b827f",
]
known_sha1 = [
  "461fcb0edb27637c720bf3734af576856bb2137a",
]

[recompiler]
seeds = "seeds/ghidra_funcs.txt"
out_dir = "generated"
strict = true

[runtime]
window_title = "Einhander Recompiled"
memcard_dir = "saves"
# Overlays : cache de compilation + backend gcc (comme le projet EinhanderRecomp).
overlay_cache = true
overlay_backend = "gcc"

[video]
renderer = "opengl"
aspect_ratio = "4:3"
# Ecran haut rafraichissement (120 Hz) : la vsync piloterait la cadence et
# ferait tourner tout le jeu a 2x (VBLANK a 120 Hz). "off" rend la cadence
# au pacer fixe 59,94 Hz.
vsync = "off"

[widescreen]
offer = false
offer_ultrawide = false

[controller]
# Einhander ne lit pas les sticks -> pad numerique. La DualSense est reconnue
# automatiquement par SDL (base de mappings integree) : croix directionnelle +
# boutons PS en 1:1, aucune config par-jeu necessaire.
default_mode = "digital"
multitap_analog = true

[audio]
buffer_ms = 30
spu_hq = false

# Online mount gate: digests + track count + optional TOC fingerprint
# (see docs/NETPLAY.md).
[netplay]
require_cue = true
required_tracks = 1
required_disc_fp = "65393df4ff611265cd99737df47f6af7c8f6798a1826d805cc31c54c1e8cb637"
